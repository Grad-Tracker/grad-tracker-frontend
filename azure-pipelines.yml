trigger:
  branches:
    include:
      - main
      - dev

pool:
  name: gradtracker-agents

stages:

  - stage: Build
    displayName: 'Build Next.js App'
    jobs:
      - job: BuildJob
        steps:
          - task: NodeTool@0
            inputs:
              versionSpec: '20.x'
            displayName: 'Use Node.js 20'

          - script: npm ci
            displayName: 'Install dependencies'

          - script: npm run build
            displayName: 'Build app'

          - task: CopyFiles@2
            displayName: 'Stage build artifacts'
            inputs:
              contents: |
                .next/**
                public/**
                package.json
                package-lock.json
                next.config.js
              targetFolder: $(Build.ArtifactStagingDirectory)

          - task: PublishBuildArtifacts@1
            displayName: 'Publish artifacts'
            inputs:
              artifactName: 'gradtracker-build'

  - stage: Deploy_Dev
    displayName: 'Deploy to Dev'
    dependsOn: Build
    condition: and(succeeded(), eq(variables['Build.SourceBranchName'], 'dev'))
    variables:
      - group: gradtracker-dev-vars
    jobs:
      - deployment: DeployDev
        displayName: 'Deploy to Dev VM'
        environment: dev
        pool:
          name: gradtracker-agents
          demands:
            - agent.name -equals GradTracker-dev-vm
        strategy:
          runOnce:
            deploy:
              steps:
                - script: |
                    export SUPABASE_URL=$(az keyvault secret show \
                      --vault-name GradTracker-dev-kv \
                      --name SUPABASE-URL \
                      --query value -o tsv)

                    export SUPABASE_ANON_KEY=$(az keyvault secret show \
                      --vault-name GradTracker-dev-kv \
                      --name SUPABASE-ANON-KEY \
                      --query value -o tsv)

                    export ANTHROPIC_API_KEY=$(az keyvault secret show \
                      --vault-name GradTracker-dev-kv \
                      --name ANTHROPIC-API-KEY \
                      --query value -o tsv)

                    export NEXTAUTH_SECRET=$(az keyvault secret show \
                      --vault-name GradTracker-dev-kv \
                      --name NEXTAUTH-SECRET \
                      --query value -o tsv)

                    sudo mkdir -p /var/www/gradtracker
                    sudo cp -r $(Pipeline.Workspace)/gradtracker-build/. /var/www/gradtracker/
                    cd /var/www/gradtracker
                    npm ci --omit=dev

                    pm2 stop gradtracker || true
                    pm2 delete gradtracker || true
                    pm2 start npm --name "gradtracker" -- start
                    pm2 save
                  displayName: 'Deploy and restart app'

  - stage: Deploy_Prod
    displayName: 'Deploy to Prod'
    dependsOn: Build
    condition: and(succeeded(), eq(variables['Build.SourceBranchName'], 'main'))
    variables:
      - group: gradtracker-prod-vars
    jobs:
      - deployment: DeployProd
        displayName: 'Deploy to Prod VM'
        environment: prod
        pool:
          name: gradtracker-agents
          demands:
            - agent.name -equals gradtracker-prod-agent
        strategy:
          runOnce:
            deploy:
              steps:
                - script: |
                    export SUPABASE_URL=$(az keyvault secret show \
                      --vault-name GradTracker-prod-kv \
                      --name SUPABASE-URL \
                      --query value -o tsv)

                    export SUPABASE_ANON_KEY=$(az keyvault secret show \
                      --vault-name GradTracker-prod-kv \
                      --name SUPABASE-ANON-KEY \
                      --query value -o tsv)

                    export ANTHROPIC_API_KEY=$(az keyvault secret show \
                      --vault-name GradTracker-prod-kv \
                      --name ANTHROPIC-API-KEY \
                      --query value -o tsv)

                    export NEXTAUTH_SECRET=$(az keyvault secret show \
                      --vault-name GradTracker-prod-kv \
                      --name NEXTAUTH-SECRET \
                      --query value -o tsv)

                    sudo mkdir -p /var/www/gradtracker
                    sudo cp -r $(Pipeline.Workspace)/gradtracker-build/. /var/www/gradtracker/
                    cd /var/www/gradtracker
                    npm ci --omit=dev

                    pm2 stop gradtracker || true
                    pm2 delete gradtracker || true
                    pm2 start npm --name "gradtracker" -- start
                    pm2 save
                  displayName: 'Deploy and restart app'