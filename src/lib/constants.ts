/** Default page size for the admin course catalog table. */
export const COURSES_ADMIN_PAGE_SIZE = 25;

/** Default page size for the student course catalog grid. */
export const COURSES_PAGE_SIZE = 52;

/** Default page size used by server-side course queries. */
export const COURSES_QUERY_PAGE_SIZE = 20;
