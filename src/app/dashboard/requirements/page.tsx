import GenEdRequirements from "@/components/requirements/GenEdRequirements";

export default function RequirementsPage() {
  // temporary mock student id
  return <GenEdRequirements studentId={1} />;
}
